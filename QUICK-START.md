# 🚀 Quick Start Guide

เริ่มใช้งาน E-commerce Dashboard ใน 5 นาที!

## ⚡ ขั้นตอนการติดตั้ง

### 1️⃣ Extract ไฟล์ (10 วินาที)
```bash
unzip ecommerce-app.zip
cd ecommerce-app
```

### 2️⃣ Setup Supabase (3 นาที)

1. ไปที่ [supabase.com](https://supabase.com) และสร้าง account
2. คลิก "New Project" สร้างโปรเจคใหม่
3. รอให้โปรเจคสร้างเสร็จ (~2 นาที)
4. ไปที่ **SQL Editor** (เมนูซ้าย)
5. คลิก "+ New Query"
6. เปิดไฟล์ `supabase-schema.sql` คัดลอกโค้ดทั้งหมด
7. วางใน SQL Editor และกด **Run** (หรือ Ctrl+Enter)
8. รอให้รันเสร็จ (~30 วินาที)

### 3️⃣ ดึง API Keys (30 วินาที)

1. ไปที่ **Settings** > **API** (เมนูล่างซ้าย)
2. คัดลอก:
   - **Project URL** (ตัวอย่าง: `https://xxxxx.supabase.co`)
   - **anon public key** (คีย์ยาวๆ)

### 4️⃣ ตั้งค่า Environment (30 วินาที)

```bash
# คัดลอกไฟล์ตัวอย่าง
cp .env.example .env

# แก้ไขไฟล์ .env ใส่ค่าจาก Supabase
# ใช้ text editor เปิดไฟล์ .env แล้วแก้:
NUXT_PUBLIC_SUPABASE_URL=ใส่ Project URL ของคุณ
NUXT_PUBLIC_SUPABASE_KEY=ใส่ anon key ของคุณ
```

### 5️⃣ ติดตั้งและรัน (1 นาที)

```bash
# ติดตั้ง dependencies
npm install

# รัน development server
npm run dev
```

เปิดเบราว์เซอร์: **http://localhost:3000**

## 🎉 เสร็จแล้ว!

### ทดสอบระบบ:

1. **สมัครสมาชิก**
   - ไปที่ `/register`
   - กรอก: ชื่อ, อีเมล, รหัสผ่าน
   - กด "สมัครสมาชิก"

2. **เข้าสู่ระบบ**
   - ระบบจะพาไปหน้า Dashboard อัตโนมัติ
   - เห็น Sidebar ด้านซ้าย
   - เห็น Stats Cards

3. **ทดสอบ Sidebar**
   - คลิกปุ่ม ☰ เพื่อย่อ/ขยาย
   - คลิกเมนูต่างๆ

## 🔐 เปลี่ยนเป็น Admin (ถ้าต้องการ)

ไปที่ Supabase **SQL Editor** รันคำสั่ง:

```sql
UPDATE profiles 
SET role = 'admin' 
WHERE email = 'your-email@example.com';
```

## ❓ มีปัญหา?

### Error: "Cannot connect to Supabase"
➡️ เช็คว่าใส่ URL และ Key ถูกต้องใน `.env`

### Error: "Database error"
➡️ เช็คว่ารัน `supabase-schema.sql` แล้ว

### Error: "npm install failed"
➡️ ลองรัน: `npm cache clean --force` แล้ว `npm install` ใหม่

### Sidebar ไม่แสดง
➡️ ลอง Hard Refresh: `Ctrl+Shift+R` (Windows) หรือ `Cmd+Shift+R` (Mac)

## 📚 อ่านเพิ่มเติม

- คู่มือฉบับเต็ม: อ่าน `README.md`
- Database Schema: ดูใน `supabase-schema.sql`

## 🆘 ขอความช่วยเหลือ

ถ้ายังมีปัญหา:
1. เช็ค Console ของเบราว์เซอร์ (F12)
2. ดู Logs ใน Terminal
3. เช็ค Supabase Dashboard > Logs

---

Happy Coding! 🚀
